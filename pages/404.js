import {Heading} from "../components/Heading";

const Error = () => {
    return <div>
        <Heading text={'404'}/>
        <Heading text={'Something went wrong...'} tag={'h3'}/>
    </div>
}

export default Error
